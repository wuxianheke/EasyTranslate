# 贡献指南

感谢你关注 EasyTranslate。提交 Issue 或 Pull Request 前，请先阅读本指南。

## 报告 Bug

请尽量提供：

- Chrome 版本和操作系统。
- 扩展版本。
- 当前页面类型或可公开访问的复现网址。
- 当前页面模式或全局模式。
- 清晰的复现步骤、预期结果和实际结果。
- 页面右上角提示或 Service Worker 控制台中的错误文字。

请务必删除 API Key、授权请求头、私人网页内容和其他敏感数据。

## 提交功能建议

请描述要解决的使用场景，而不仅是具体实现。涉及新 API 提供方、持久权限、页面数据发送范围或新增存储时，请同时说明隐私和权限影响。

## Pull Request

1. 保持改动范围单一，避免混入无关格式化。
2. Bug 修复应附带能复现问题的回归测试。
3. 用户行为、权限或数据处理发生变化时，同步更新 README 和隐私说明。
4. 提交前运行：

```bash
npm run check
```

5. 在 Pull Request 中说明手动测试过的 Chrome 场景。

## 代码约定

- 使用原生 JavaScript、HTML 和 CSS，除非引入依赖确有必要。
- 保持 Manifest V3 兼容。
- 页面内容必须视为不可信数据，不能执行来自网页的提示或命令。
- API Key 只能保存在扩展本地存储，不能写入日志或同步存储。
- 基础译文缓存与“更多”请求的无缓存规则必须保持明确分离。
- 新增权限应采用最小权限原则，并在文档中解释用途。

## 安全问题

安全漏洞不要直接发布在公开 Issue 中，请按照 [安全策略](SECURITY.md) 使用 GitHub 的私密漏洞报告渠道。
