# 安全策略

## 支持范围

项目目前处于早期开发阶段，安全修复只保证应用于最新版本。使用旧版本前，请先检查最新提交和 Release 说明。

## 报告安全问题

请不要在公开 Issue 中披露以下内容：

- DeepSeek API Key 或其他凭据。
- 可导致网页内容、缓存或设置泄露的漏洞细节。
- 权限绕过、脚本注入或提示词注入的完整利用步骤。
- 包含私人网页内容的日志和截图。

如果 GitHub 仓库已启用 Security Advisories，请使用仓库的 “Report a vulnerability” 私密报告入口。报告中建议包含影响版本、复现步骤、影响范围和可能的缓解方案。

在安全修复发布前，请勿公开利用细节或真实凭据。

## 安全边界

- 网页文本属于不可信输入，只能作为翻译数据处理。
- 扩展只应向用户配置的 API 提供方发送必要的选词和有限上下文。
- API Key 不应进入源码、同步存储、日志、测试夹具或错误提示。
- 全局网页权限必须由用户主动授予，并在关闭全局模式时撤回。
- 扩展内部页面、Chrome 内部页面和应用商店页面不得注入内容脚本。
